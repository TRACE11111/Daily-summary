    早上在上机器学习的课
    下午在看昨天没有看完的那篇论文，重点看了异常检测的那一部分，这篇论文所用到的方法和基线方法不同，基线方法是将所有的参数值向量序列存储到一个矩阵中，其中每一列都是来自一个日志关键字k
的参数值序列，每一行代表的是时间实例，但是这种方法当存在许多对数键值和或是存在一些大的参数值向量时，矩阵仍然会非常稀疏。此外，这种方法会给异常检测过程带来延迟，并且很难为每个范围的长度确定
一个好的值。而这篇论文提出的方法是通过DeepLog将每个参数值向量序列(对于日志关键字)视为单独的时间序列来训练参数值异常检测模型。而预测和观测参数值向量之间的差异是通过均方误差(MSE)来测量。
    而且其异常检测的模型是可以在线更新的，方法有点儿像涛涛师兄下午说的那种方法，这篇论文所提出来的DeepLog模型是不需要从头开始重新训练的，在初始训练过程之后，DeepLog中的模型作为多个多维权
重向量存在。更新过程输入新的训练数据，并调整权重以最小化模型输出和来自假阳性情况的实际观察值之间的误差。
    下午还开了组会，感觉师兄们还是厉害啊，虽然有些听的不是太懂，但是也学到了一些东西，回来后专门搜了什么是“概念漂移”，在机器学习领域中，这个概念指的就是一个模型要去预测的一个目标变量，
概念漂移就是这个目标变量随着时间的推移发生改变。
     晚上在看机器学习的视频。
